server:
 port: 8080
spring:
  application:
    name: springcloud-order
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://admin:eureka@localhost:8761/eureka
  instance:
      instance-id: springcloud-order #	页面Status显示昵称
      prefer-ip-address: true #显示IP 而非主机名称
      lease-renewal-interval-in-seconds: 2 #设置心跳的时间间隔 默认30s
      lease-expiration-duration-in-seconds: 5 # 多长时间没收到心跳就清除这个实例 默认90s
info:
  app.name: springcloud-order
  company.name: NiuShi group
  build.artifactId: $project.artifactId$
  build.modelVersion: $project.modelVersion$

